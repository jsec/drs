pre-push:
  parallel: true
  commands:
    go-lint:
      glob: "*.go"
      run: task lint

    web-lint:
      root: "web/"
      run: pnpm run lint

    etl-check:
      glob: "etl/**"
      run: task etl:check
