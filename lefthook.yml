pre-push:
  parallel: true
  commands:
    go-lint:
      glob: "*.go"
      run: task lint

    web-lint:
      glob: "web/**"
      root: "web/"
      run: pnpm run lint

    etl-check:
      glob: "etl/**"
      run: task etl:check
