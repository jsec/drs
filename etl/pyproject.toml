[project]
name = "drs-db"
version = "0.1.0"
requires-python = ">=3.13,<3.14"
dependencies = [
    "dbt-core>=1.10,<2",
    "dbt-postgres>=1.9,<2",
]

[tool.uv]
package = false

[tool.sqlfmt]
line_length = 120
exclude = ["dbt/target/**/*", "dbt/dbt_packages/**/*", ".venv"]
