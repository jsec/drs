[project]
name = "drs-db"
version = "0.1.0"
requires-python = ">=3.13,<3.14"
dependencies = [
    "dbt-core>=1.10,<2",
    "dbt-postgres>=1.9,<2",
    "httpx2>=2.5.0",
    "psycopg[binary]>=3.3.4",
    "pygithub>=2.9.1",
    "rich>=15.0.0",
]

[tool.uv]
package = false

[tool.ruff]
line-length = 120
target-version = "py313"

[tool.ruff.lint]
select = ["B", "C4", "E", "F", "I", "PLC", "PLE", "PLW", "SIM", "UP", "W"]
ignore = ["E501"]

[tool.ruff.format]
indent-style = "space"
quote-style = "double"

[tool.sqlfmt]
line_length = 120
exclude = ["dbt/target/**/*", "dbt/dbt_packages/**/*", ".venv"]

[dependency-groups]
dev = ["pyrefly>=1.1.1", "ruff>=0.15.19"]
